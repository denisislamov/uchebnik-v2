# Проверка полного прототипа

> Последняя регрессия от 25.09.2026 — эпик E6 «Тексты и понятность» ([план](../review/review_1_plan.md)): контент перегенерирован (заголовки по роли/виду задания, «Реши примеры и запиши ответы.» для 243 столбиков), `npm run test:all` — контент `--check`, TypeScript, 638 Node-тестов, release-guard, web export и 19 браузерных наборов PASS. Линтер текстов: 488 → 245 блоков с замечаниями ([TEXT_LINT.md](TEXT_LINT.md)). Предыдущие: e5 «Шапка и вёрстка» ([план](../review/review_1_plan.md)): `npm run test:all` — контент `--check`, TypeScript, 638 Node-тестов, release-guard, web export и 19 браузерных наборов PASS (добавлен `layout-smoke`). Предыдущие: e4 «Статус выполнения» ([план](../review/review_1_plan.md)): `npm run test:all` — контент `--check`, TypeScript, 638 Node-тестов, release-guard, web export и 18 браузерных наборов PASS (добавлен `status-smoke`). Предыдущие: e3 «Мелкие баги» ([план](../review/review_1_plan.md)): `npm run test:all` — контент `--check`, TypeScript, 638 Node-тестов, release-guard, web export и 17 браузерных наборов PASS (добавлен `search-smoke`). На каждом из 1323 блоков: один элемент заголовка, задание не дублирует заголовок, символа `□` в тексте страницы нет. Предыдущие: e2 «Мелкие цели на телефоне» ([план](../review/review_1_plan.md)): `npm run test:all` — контент `--check`, TypeScript, 636 Node-тестов (+7: физические допуски и размер клетки), release-guard, web export и 16 браузерных наборов PASS. Найдено и исправлено по ходу: у практических листов был собственный горизонтальный скроллер с клеткой 28 px, и вместе с новым листом поля цель уезжала за край — листом теперь владеет только `DrawingPad`; проверка поворота экрана в `mobile-coach-smoke` переписана с фиксированных 700 мс на ожидание подсветки (ложные падения в каждом втором прогоне). Предыдущие: e1 «Подсказки по кнопке» ([план](../review/review_1_plan.md)): `npm run test:all` — контент `--check`, TypeScript, 629 Node-тестов, release-guard, web export и 16 браузерных наборов PASS; 143 страницы / 1323 блока, на каждом ровно одна кнопка «Как это сделать?» и ни одного самопроизвольного обучения. Найдено и исправлено по ходу: карточка объяснения на первом кадре позиционировалась по угаданной высоте и вылезала за экран; на web `Modal` возвращал фокус кнопке после закрытия и сбивал восстановленную прокрутку (сигнал теперь из `onDismiss`); в `drawing-guidance-smoke` добавлен допуск 1 px на округление прокрутки. Предыдущая: базовая линия от 25.09.2026 перед работами по отзывам тестеров ([сводка](../review/review_1_summary.md), [план](../review/review_1_plan.md), эпик E0). `npm run test:all` на `8d70062`: контент `--check`, TypeScript, 627 Node-тестов, release-guard, web export и 16 браузерных наборов PASS; 143 страницы / 1323 блока. Эталонные снимки страницы 3 (шаги 1, 2, 6; 390 и 1280 px; с открытой подсказкой и после её закрытия) сняты с опубликованной сборки того же коммита: [review-1-baseline/](review-1-baseline/). Они нужны для сравнения после эпиков E1, E4, E5. Предыдущая регрессия: последняя регрессия от16.09.2026: [детские подсказки и взаимодействия](CHILD_INTERACTION_FIXES.md), 562 теста и 9 браузерных сценариев PASS; 143 страницы / 1323 блока. Ниже сохранён исторический отчёт. Исправления по сканам от15.09.2026: [SOURCE_FIX_RESULTS.md](SOURCE_FIX_RESULTS.md).

Дата актуальной проверки: 2026-09-13. Локальная web-версия: `http://127.0.0.1:8081/`.

| Проверка | Результат |
|---|---|
| TypeScript | PASS |
| Node test runner | 304 теста PASS |
| Номера заданий | 1–892 без пропусков; продолжения № 331/489/887 представлены на обеих страницах |
| Ресурсы | 488 иллюстраций и 144 скана существуют и включены в приложение |
| Все поля | Для каждого блока `work` проверены полный ответ, пустое поле, изменение ответа и необходимость проверки |
| Открытые примеры | Для каждого набора правил найдены разные допустимые решения; невозможных наборов нет |
| Известные контрольные ответы | № 198, 401, 489, 499, 678, 892 сверены отдельными фиксированными ожиданиями |
| Прописи | Все статические планы выполнимы; неверные цвет, длина, направление, положение и неполный штрих отвергаются |
| Привязка к клеткам | Стороны/диагонали имеют целочисленные координаты; смещение на полклетки отвергается |
| Браузер, полный каталог | 143 доступные страницы и 1331 шаг открыты (титул исключён); изображения загружены, поля доступны, runtime-ошибок нет |
| Дополнительные мобильные сценарии | 9 сценариев: составление задачи, состав числа, счёт, десятки/единицы, весы, жидкость, сотня, поиск страниц, сохранение |
| Актуальность генерации | Markdown → source_blocks.json → fullBookData.ts: обе проверки `--check` PASS |
| Браузер, первые страницы | Повторно пройдены старые сценарии: 81 блок, выбор на рисунках, цифры, 23 элемента прописи, восстановление, отмена и touch |
| Web export | PASS |
| iOS / Android export | PASS: сборка JS/Hermes и ресурсов; это не запуск на устройствах |

Машинные результаты: [все доступные шаги](all-pages-browser-result.json), [механики полного курса](full-course-browser-result.json), [начальные страницы](browser-result.json), [дополнительные мобильные сценарии](activity-browser-result.json).

Актуальный локальный прогон выполнен в Playwright Chromium с отдельным тестовым профилем. [Как запускать автотесты и CI](AUTOMATED_TESTING.md) · [Инструкция ручной проверки](MANUAL_TESTING.md) · [Постраничный чек-лист](MANUAL_PAGE_CHECKLIST.md). GitHub Actions настроен; результат удалённого прогона следует смотреть в Actions для нужного коммита.

## Визуальная проверка

Проверены снимки с отдельными ответами, игрой и прописями. На 390 px стороны квадрата и диагональ совпадают с линиями/углами клетки. Правильный жест мыши проходит, смещённый на полклетки — нет. Прежний браузерный touch-сценарий тоже пройден.

- [Чёрточка на стороне клетки](full-course-grid-mobile.png)
- [Квадрат и диагональ](full-course-square-mobile.png)
- [Неизвестные слагаемые](full-course-addends.png)
- [Игра с мишенью](full-course-game.png)

## Границы результата

- Нативные экспорты подтверждают сборку, но не работу в Expo Go или на физическом iOS/Android. VoiceOver/TalkBack и системный русский голос на устройствах не проверены.
- Ребёнок не участвовал в тестировании. Допуски геометрии следует проверить на реальном детском почерке и сенсорном экране.
- Наличие всех номеров и автоматическая проверка данных не доказывают ручную редакторскую сверку каждого из тысяч подпунктов. Контрольные ответы и наиболее неоднозначные случаи проверены отдельно; исходное ТЗ остаётся доступно.
- Открытые задания проверяются по структуре и арифметике; связность произвольного рассказа не распознаётся.
- Измерения используют явно заданные виртуальные параметры. Размеры комнаты, тетради и одежды ребёнка приложение не определяет.
- Рисование означает проверяемое обведение шаблона. Это не распознавание произвольного рисунка.
- Мешки на странице 5 остаются свободной тренировкой: перекрытия в скане не позволяют навязать одно точное число.

## Исправления прописей после сверки со сканами

- Страница 6: только 12 чёрных волн, без добавленных зелёных точек.
- Страница 12: полотнище флажка 2×1 клетки с вырезом; последний флажок зеркальный.
- № 298 и 353: вертикальные непрерывные столбики на общей нижней линии; для 20 клеток лист увеличивается до 22 рядов. Рендер и проверка используют размеры одного плана.
- Добавлены три регрессионных теста по этим требованиям; флажок и столбики просмотрены в открытой пользовательской вкладке.

## Ошибки переноса, выявленные новыми проверками ТЗ

- № 642: восстановлено деление 18 : 2, добавлены шесть вычислений из рамки; всего 14 отдельных ответов.
- № 798: восстановлен третий столбик обычных примеров рядом с выражениями с пропусками.
- Извлечение больше не удаляет деление после русского союза «и»; смешанные блоки сохраняют оба вида выражений.
- 144 теста проверяют постраничный состав упражнений и иллюстраций. Дополнительные тесты сравнивают распознаваемые печатные примеры из ТЗ с полями ответа, включая повторные вхождения.

## Подсказки направления — 10 сентября 2026

- Синие стрелки следуют порядку точек текущего штриха: прямые, диагонали, замкнутые фигуры, кривые и цифры. Общий компонент используется и для столбиков клеток.
- Над полем объясняется значение стрелок; для точки вместо направления показана инструкция коснуться кружка без движения.
- TypeScript и 279 Node-тестов PASS; локальный браузерный прогон 144 страниц / 1345 блоков PASS. Прогон проверяет наличие подсказок и стрелок в каждом статическом задании на рисование.
- Проверен свежий мобильный снимок квадрата: стрелки направлены вдоль всех четырёх сторон. Этот результат относится к локальной версии; отдельный GitHub CI для изменения ещё не запускался.

## Режим сверки — 10 сентября 2026

Ветка `feature/debug-source-view`: локальный `web:debug` на порту 8082 показывает задание и полный скан рядом. Проверены смена страницы и шага, увеличение, независимая прокрутка, скрытие на главной и узкая раскладка. [Инструкция](DEBUG_SOURCE_VIEW.md) и [снимок](debug-source-view.png).

TypeScript, 279 Node-тестов и обычный браузерный прогон 144 страниц / 1345 блоков — PASS. Панель отсутствует на всех шагах production web. Проверка `test:release-guard` с намеренным `EXPO_PUBLIC_SOURCE_DEBUG=1` — PASS для web, iOS и Android: маркеры debug-панели отсутствуют в JS и Hermes-байткоде. Нативные экспорты не означают запуска на физических устройствах. Эти результаты локальные; CI новой ветки ещё не запускался.

## Ручные замечания — итог 10 сентября

Обложка вынесена из уроков, титул скрыт, счёты заменены карточками. Все 12 заданий с палочками используют перетаскивание. На рисунке с шашками выбираются шесть парных объектов. Замкнутые контуры допускают начало с любого места в обе стороны. Обновлены палитра, сохранение цветов и траектории всех цифр по оригиналам, включая отдельное расположение «10».

Проверены 295 Node-тестов, реальные жесты мыши и touch, источники и [наложение цифр](handwritten-digit-overlay.png). Скриншоты: [парные предметы](room-pairs.png), [фигура из палочек](sticks-drag-drop.png). Общее наличие всех 144 исходных страниц сохраняется в данных и ТЗ, но в интерфейсе доступны 140 учебных и 3 дополнительные страницы. Нативная работа с ребёнком на физических устройствах требует отдельной ручной проверки.

## Замечания 13 сентября — регрессия

`test:content`, `typecheck`, `test` (304), `test:release-guard` (web/iOS/Android), `export:web` — PASS. После обновления старых сценариев на перетаскивание все шесть браузерных наборов прошли: начальные страницы, полный курс, дополнительные мобильные механики, прежние детские взаимодействия, новые замечания, все доступные шаги (143 страницы / 1331 шаг). Нативный экспорт не означает запуска на физическом телефоне.

Новый браузерный сценарий проверяет перенос и возврат предмета, отсутствие добавления по клику, touch, палочки, две независимые группы, три квадратика по образцу, восстановление корректного и повреждённого сохранения, четыре вопроса о расположении, попадание рядом с ягодой, ствол в обоих направлениях, обе сливы и подпись вместе, полные условия PDF 11 и равенство отрисованных сторон треугольника на ширине 390/1440. Сценарий дополнительных механик проходит новый способ раскладки квадратиков 1+2 через перетаскивание. Старый набор конструкций проходит все 12 заданий, неверное размещение, повтор, отмену и сохранение.

Иллюстрации проверены визуально: [кружок на телефоне](counter-drag-mobile.png), [квадратики](page11-square-layout.png), [условия PDF 11](page11-condition.png), [две сливы вместе](plums-together.png). Независимое ревью выявило и помогло исправить повторное масштабирование палочек, некорректные индексы сохранённых предметов и две ошибочные привязки иллюстраций. Повторная проверка этих исправлений не нашла блокирующих ошибок.

Нумерация исходных страниц и все 892 номера сохранены. Удалён редакторский шаг «Образы числа», а его материалы прикреплены к подходящим заданиям. Для сохранений до изменения каталога добавлена миграция индекса шага; старое несовместимое начертание требует повторения с видимым объяснением.

Отдельный `test:debug` — PASS: задание/PDF рядом, синхронизация страницы и шага, увеличение, главная, узкая раскладка. Оставшийся от прошлого сеанса Metro имел устаревший кэш разрешения `expo-asset`; после перезапуска с `--offline --clear` на прежнем порту 8082 режим прошёл проверку. Сервер оставлен запущенным.

## Наглядное обучение новым заданиям — 16 сентября 2026

Свежий локальный `TEST_PORT=8096 npm run test:all` завершился с кодом 0: проверка контента, TypeScript, **589 Node-тестов**, production guard web/iOS/Android, web-export и все **9 браузерных наборов** — PASS. Просмотр всех доступных шагов: **143 страницы / 1323 блока**. Набор обучения проверил 7 групп сценариев; набор стрелок — 2 ширины дерева, 2 направления touch и 6 проверок первого штриха/цифр.

Реестр содержит 152 смысловых семейства; первое знакомство определяется видом учебной работы, а не только способом ввода. Движущийся палец привязан к предметам/траекториям/местам переноса. Просмотр не изменяет ответы. Проверены сохранение, повтор, пауза, короткая горизонтальная раскладка, все десять детей исходного рисунка и выбор числа 10. Стрелки уменьшены, отнесены от контура и скрываются во время собственного штриха.

Описание, границы и ручная приёмка: [TASK_TEACHING_RESULTS.md](TASK_TEACHING_RESULTS.md). Машинные результаты: [teaching-browser-result.json](teaching-browser-result.json), [drawing-guidance-browser-result.json](drawing-guidance-browser-result.json). Просмотрены свежие снимки счёта, рисования, дерева, переноса и выбора ответа в горизонтальном окне. Физические iOS/Android, озвучивание на устройстве и понятность объяснений ребёнку — **NOT RUN**. GitHub CI для текущих локальных изменений не запускался.

## Кнопка подсказки и обрезанная иллюстрация — уточнение 16 сентября

«Покажи движение» заменено на «Покажи подсказку». Подсветка шагов с единственной исходной иллюстрацией привязана к самой картинке, а не к обёртке с кнопками. Если целая картинка не помещается над текстом, показ использует копию с сохранением пропорций. Исправлен также правый край подсветки в коротком горизонтальном окне. Для сравнения размера/длины убраны отвлекающие схемы количества кружков; остаются сами предметы рисунка.

После этих изменений: TypeScript и 589 Node-тестов — PASS; свежий web-export — PASS; `coach-layout-smoke` (1280×800, 390×844, 800×375, 320×568), `teaching-smoke` (7 групп) и `all-pages-smoke` (143 страницы / 1323 блока) — PASS. Новый layout-тест сначала воспроизвёл дефект падением, затем прошёл после исправления. Устранена гонка самого теста при определении последней кнопки во время смены модального окна. Нативный запуск для этого уточнения не проверялся; полный `test:all` повторно не запускался. Новый layout-набор включён в будущие полные запуски (теперь 10 браузерных наборов).

## Итог повторного аудита — 16 сентября 2026

Все слои проверены на текущем коде отдельными запусками, без повторного монолитного `test:all`:

- `test:content`, `typecheck`, **607 Node-тестов**, `export:web` — PASS.
- `test:release-guard` — PASS: production web/iOS/Android исключают панель оригинала даже с включённым debug-флагом. Это экспорт, не запуск на телефоне.
- Все **12 браузерных наборов** стандартного прогона — PASS. Совместимый вход `gesture-coach-smoke.cjs` вызывает проверенный `teaching-smoke.cjs`; полный аудит запущен отдельно для каждой ориентации.
- Онбординг: **159 семейств, 893 шага в каждой ориентации**, **1786 проверок шагов суммарно**, 0 ошибок. Размеры окон: 390×844 и 800×375. Показ не изменяет ответы ребёнка.
- Отдельные проверки: 7 групп обучения, 4 размера окна для целой иллюстрации, 6 регрессий жестов/повторного показа. Проверены реальные нажатия на оба стула и перетаскивания предметов/цифровых карточек.
- Общая проверка отображения курса: **143 страницы / 1323 блока** — PASS. Она не означает решение всех заданий через интерфейс.

[Подробный аудит и ограничения](ONBOARDING_AUDIT.md). Физические iOS/Android, озвучивание на устройстве, VoiceOver/TalkBack и понимание объяснений ребёнком — **NOT RUN**. GitHub CI текущих локальных изменений не запускался.

## Количество и цифра — 16 сентября 2026

Для вводных заданий PDF 7, 8, 10 устранён ошибочный выбор только цифры: предметы, точки, кружки и запись числа теперь изучаются вместе. `test:content`, TypeScript, 611 Node-тестов и свежий web-export — PASS. Новый браузерный набор (6 случаев), все 50 шагов новых подсказок в двух ориентациях, регрессия начальных страниц и просмотр курса (143 страницы / 1323 блока) — PASS. Полный `test:all` после этой ограниченной правки не повторялся. Подробности: [NUMBER_MEANING.md](NUMBER_MEANING.md). Подбор голоса подготовлен в [VOICE_SELECTION.md](VOICE_SELECTION.md); новые записи не генерировались и провайдер в приложении не заменён.

## Два задания на состав числа — 16 сентября 2026

По уточнению пользователя задания PDF 11 сохранены как два последовательных разложения: сначала 3 = 2 + 1, затем 3 = 1 + 2. Оба используют одинаковые два поля и перетаскивание квадратиков. Вариант со свободным расположением фигур отменён. Аналогичные пары для чисел 4–10 используют тот же интерфейс: состав по образцу и другой состав. Подсказка объясняет число текущего задания вместо общего примера с пятью предметами.

- `test:content`, `typecheck`, **612 Node-тестов**, `export:web` — PASS.
- Новый браузерный набор: **9 случаев / 18 заданий** — PASS (все восемь пар 3–10 на 390×844, тройка также на 800×375). Проверены реальные переносы, неверный порядок частей для тройки, исправление и восстановление результата. [Отчёт](composition-pairs-browser-result.json).
- Регрессия ручных замечаний, **9 дополнительных мобильных сценариев** и **6 регрессий жестов/подсказок** — PASS отдельными запусками.
- Аудит двух затронутых подсказок: **28 шагов суммарно**, по 14 в каждой ориентации — PASS. [390×844](composition-coach-390.json), [800×375](composition-coach-800.json). Полные исторические отчёты аудита остальных подсказок не заменялись частичным прогоном.

Полный `test:all`, GitHub CI и проверка на физических iOS/Android после этой правки — **NOT RUN**. Новый набор включён в последующие полные запуски: всего 14 браузерных наборов.

## Онбординг без мерцания и сдвигов — 17 сентября 2026

Причина мерцания воспроизведена покадровым браузерным тестом: прежний код закрывал модальное затемнение на время измерения следующего шага (21–22 кадра без слоя в проверенных случаях). При открытии также менялись высота страницы и положение рисунка из-за дополнительного отступа и автоматической прокрутки.

В браузере слой теперь остаётся открытым до завершения/закрытия подсказки. Новая цель и текст переключаются после измерения вместе; прежняя подсветка сохраняется на время ожидания. Страница не получает новых отступов и не прокручивается сама. Увеличенные копии задания отключены: выделяются настоящие элементы, карточка объяснения ищет место вокруг текущей цели. Маска и указатель ограничены видимой областью прокрутки и не выделяют закреплённую шапку или скрытый за ней предмет. Если нужное место не видно, ребёнок может нажать «Показать это место». Только этот явный переход прокручивает страницу; он не сбрасывает паузу или момент анимации и не меняет ответы.

Подтверждённые проверки последней сборки:

- `typecheck`, `test:content`, **614 Node-тестов**, `export:web` — PASS.
- Покадровая проверка на 1440×900, 1280×800, 390×844 и 800×375: **0 пропаданий слоя, 0 автоматических сдвигов**; размеры исходного рисунка и высота страницы неизменны при открытии, смене шага и закрытии. Отдельно проверен явный переход к скрытому участку. [Отчёт](coach-stability-browser-result.json).
- Привязка подсветки к исходному рисунку на четырёх размерах окна — PASS. В коротком окне проверяется видимая часть настоящего рисунка, без его уменьшения/подмены. [Отчёт](coach-layout-browser-result.json).
- **6 регрессий жестов** — PASS: стулья, обе ориентации раскладки, цифровые карточки, настоящая геометрия палочек, повтор на заполненном поле. [Отчёт](onboarding-gestures-browser-result.json).
- **7 групп обучения в реальном времени** — PASS: все десять детей, пауза/повтор, перетаскивание, рисование, новые типы заданий и горизонтальное окно. [Отчёт](teaching-browser-result.json).
- Полный обход **161 семейства / 913 шагов в каждой ориентации**, всего **1826 проверок шагов** — PASS, 0 ошибок. Проверены цель/пример либо явный переход к скрытому участку, отсутствие указателя и маски за закреплённой шапкой, сохранение знакомства и неизменность ответов. [390×844](onboarding-audit-390.json), [800×375](onboarding-audit-800.json).

Новый тест стабильности включён в стандартный прогон: **15 браузерных наборов**. Полный монолитный `test:all`, GitHub CI, Safari/Firefox и физические iOS/Android после этой правки — **NOT RUN**. Результаты выше относятся к Chromium; понимание подсказок ребёнком требует ручной приёмки.

## Состав числа на общем двухцветном поле — 17 сентября 2026

Последующая правка заменяет описанные выше два поля одним. В восьми парах заданий PDF 11, 13, 15, 19, 23, 25, 27, 29 ребёнок переносит предметы двух цветов на общее поле. Квадраты соединяются сторонами в одну полоску; части видны одновременно. Цвета сверены с изображениями: зелёный/красный, а на PDF 15, 25, 27 — красный/зелёный. Два последовательных задания («по образцу» и «как ещё») сохранены. В № 72, 99, 121, 141, 155 тот же интерфейс заменяет прежние раздельные поля с квадратиками; в оригинале этих устных вопросов нет цветного образца, зелёный/красный здесь служат учебной наглядностью приложения. Итого изменено **21 задание**.

Предметы автоматически встают рядом, общее количество ограничено условием. Предмет можно вернуть вниз или отменить последнее действие. Обучающий пример и демонстрация перетаскивания используют те же цвета и общее поле. Задания с двумя блюдцами/тарелками/коробками сохраняют отдельные ёмкости согласно своему условию.

Проверки:

- `typecheck`, `test:content`, **617 Node-тестов**, свежая `export:web` — PASS.
- **17 браузерных случаев / 29 выполненных заданий**: восемь пар на 390×844, пары для 3 и 10 дополнительно на 800×375 и 1280×900, пять поздних упражнений — PASS. Проверены цвета, соприкосновение квадратов, правильные и неверные варианты для тройки, предел количества, возврат, отмена, восстановление после перезагрузки. [Отчёт](composition-pairs-browser-result.json).
- `manual-feedback-smoke.cjs` — PASS, включая touch-переносы на новом поле, простой тап без добавления и восстановление повреждённого сохранения; `activity-smoke.cjs` — PASS, 9 сценариев.
- **6 регрессий жестов**, **4 окна стабильности подсказок** и **24 шага** двух затронутых первых подсказок в двух ориентациях — PASS. [Жесты](onboarding-gestures-browser-result.json), [стабильность](coach-stability-browser-result.json), [390×844](composition-coach-390.json), [800×375](composition-coach-800.json).
- Визуально проверены десять соседних квадратов на [узком экране](composition-ten-390.png) и [десктопе](composition-ten-1280.png).

Ручная приёмка: открыть PDF 29, разложить 5 зелёных и 5 красных квадратов, вернуть один и отменить возврат; затем перейти к «Как ещё…» и собрать другой состав на таком же поле. На PDF 15 проверить обратный порядок цветов: 4 красных и 1 зелёный. На PDF 11 проверить обе последовательные раскладки: 2 + 1, затем 1 + 2.

Проверки браузера выполнены в Chromium на изолированных портах, без изменения пользовательского прогресса. Полный `test:all`, GitHub CI и физические iOS/Android для этой правки — **NOT RUN**.

## Огурцы, крючки, ранние объяснения и расположение квадратов — 17 сентября 2026

Этот раздел уточняет предыдущую раскладку полоской после повторного замечания пользователя.

- **PDF 5:** в миске и перед ней изображены 7 огурцов. Исправлены ответ задания, Markdown страницы, реестр изображений, исходные метаданные и подпись приложения. Браузерный тест переносит 6 кружков и получает отказ, затем добавляет седьмой и получает успех.
- **PDF 6:** крючок строится непрерывными кривыми: внутренний завиток, закруглённая внешняя дуга и изгибающийся наклонный хвост. Прежний скачок от дуги к прямому отрезку устранён; верх и низ находятся на линиях сетки на расстоянии двух клеток. Обводка первого крючка мышью принята и сохранена. [Контур](source-hook-before.png), [обводка](source-hook-after.png).
- **Ранний онбординг:** птицы на PDF 9 объясняются через одну сидевшую и ещё одну прилетевшую птицу. Убраны посторонние числа из первых практических задач, счёта и монет. Счётный пример использует количество текущего задания. До PDF 16 подсказки не вводят арифметические равенства; под ранними раскладками написано «и», «всего». [Подсказка про птиц](source-birds-onboarding.png).
- **Квадраты по образцу:** первое задание на PDF 11 — вертикальная зелёная пара и красный квадрат справа внизу; PDF 13 — два столбца; PDF 15, 19, 23 — исходные группы в два ряда; PDF 29 — две группы по пять с центральными квадратами. Это одно поле, а не отдельные ёмкости. «Как ещё…» сохраняет общую полоску для другого состава. Обучающий пример и указатель используют ту же геометрию. [Тройка](composition-source-three-390.png), [десятка](composition-ten-390.png).

Подтверждённые отдельные проверки: TypeScript, **623 Node-теста**, актуальность контента, web-export и исключение debug-панели из production web/iOS/Android — PASS. Новый браузерный набор из трёх сценариев — PASS. [Отчёт](source-feedback-browser-result.json). Аудит затронутых ранних подсказок: **16 семейств / 68 шагов в каждой ориентации**, суммарно **136 шагов**, без ошибок и изменения ответов. [390×844](source-early-onboarding-390.json), [800×375](source-early-onboarding-800.json).

Полная браузерная регрессия (15 стандартных наборов и отдельно новый набор исправлений, всего 16) — PASS. Полный онбординг: **161 семейство / 900 шагов в каждой ориентации**, всего **1800 шагов**, без ошибок. Отображение курса: **143 доступные страницы / 1323 блока** — PASS. После финального уточнения текстов про птиц и вопрос «Сколько осталось?» повторены Node-тесты, TypeScript, web-export, новый браузерный набор и аудит всех 16 затронутых ранних семейств в обеих ориентациях — PASS.

Проверки на настоящих iOS/Android, Safari/Firefox, понимание подсказок ребёнком и GitHub CI — **NOT RUN**. Ручные шаги добавлены в [руководство](MANUAL_TESTING.md).

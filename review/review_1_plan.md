# План работ по тестам, раунд 1

Декомпозиция [review_1_summary.md](review_1_summary.md) на эпики → задачи → подзадачи. Порядок эпиков — рекомендуемый: сначала дешёвое и заметное, чтобы позвать тестеров второй раз до начала озвучки.

**Ветка:** `feature/review-1` от текущего `feature/debug-source-view` (там живёт редизайн «тетради»). Один эпик — минимум один коммит; большие задачи (E1.2, E7) — по коммиту на задачу. После E3 и после E7 — PR в `main`. Выкладка на Render идёт с ветки `feature/debug-source-view` ([DEPLOY_RENDER.md](../docs/DEPLOY_RENDER.md)), пуш в `main` сайт не обновляет — для публикации `feature/review-1` вливается ещё и в неё (либо в настройках Render переключается ветка).

**Правило:** новый тест сначала воспроизводит дефект (падает), потом проходит после исправления. Так принято в проекте ([AUTOMATED_TESTING.md](../docs/AUTOMATED_TESTING.md)); это и есть доказательство «не хуже, чем раньше».

---

## Ворота эпика (одинаковые для всех)

Эпик закрыт, когда выполнено всё:

- [ ] `npm run test:all` зелёный локально (контент `--check`, `tsc`, Node-тесты, release-guard, `export:web`, 16 браузерных наборов на порту 8094).
- [ ] Новые тесты эпика добавлены в `npm test` или в список `scripts/run-browser-tests.cjs`; в [AUTOMATED_TESTING.md](../docs/AUTOMATED_TESTING.md) — абзац, что именно они проверяют.
- [ ] Ручная прокликка затронутых страниц на 390 и 1280 по [MANUAL_PAGE_CHECKLIST.md](../docs/MANUAL_PAGE_CHECKLIST.md): страница 3 (шаги 1, 2, 6), страница 11, одна страница из «второго десятка».
- [ ] Не изменены строки, на которые завязаны тесты, кроме тех, что меняются осознанно вместе с тестами (список в E1.2).
- [ ] Коммит с описанием «что и почему», push, CI (`.github/workflows/test.yml`) зелёный.
- [ ] Результаты прогона записаны в [VALIDATION.md](../docs/VALIDATION.md) (дата, что прогнано, что упало и было починено).

Если ворота не проходят — эпик не закрывается, следующий не начинается.

---

## E0. Базовая линия — до первого изменения

Без неё «не хуже, чем раньше» не с чем сравнивать.

- [x] **E0.1** Прогнать `npm run test:all` на текущем HEAD (`8d70062`), зафиксировать результат в VALIDATION.md. Если что-то уже красное — записать как известное, не чинить в этом эпике.
- [x] **E0.2** Создать ветку `feature/review-1`, закоммитить `review/review_1.txt`, `review_1_summary.md`, этот план.
- [x] **E0.3** Снять эталонные скриншоты страницы 3 (шаги 1, 2, 6) на 390 и 1280 в `docs/review-1-baseline/` — для визуального сравнения после E1, E4, E5.

**Объём:** S. **Коммит:** «Add round-1 tester feedback, summary and plan».

---

## E1. Подсказки

Закрывает пункт 2 сводки (шесть замечаний Тани и Алексея). Самый частый источник раздражения; чинится точечно.

### E1.1 Выключить автопоказ

- [x] a. Убрать эффект автозапуска в [`GestureCoach.tsx:392-399`](../src/components/GestureCoach.tsx:392) (поиск первой «невиданной» семьи и `replay`). `replay` по кнопке остаётся.
- [x] b. Решить: оставить ли автопоказ только для самого первого задания сессии. Предлагаю **нет** — обе жалобы про «раздражает» и «и так понятно». Если да — один флаг `firstTaskOnly`, не больше.
- [x] c. Хранилище `uchebnik:gesture-coach:v2` больше не управляет показом — оставить для истории «смотрел / не смотрел» (может пригодиться для E8 ачивок), либо удалить вместе с `readSeenCoaches`. Не оставлять мёртвый код.
- [x] d. **Тест (браузер):** чистая история → открыть страницу 3, шаг 1 → подсказки нет, кнопка есть. Это инверсия текущего `gesture-coach-smoke.cjs`, где чистая история ожидает автопоказ — обновить его, не выкидывать: сценарии «нажал кнопку → показалось → шаги → закрыл» остаются.
- [x] e. Пересмотреть `browser-context.cjs:22-25`: сидирование «обучение пройдено» в остальных наборах становится ненужным. Убрать, чтобы наборы проверяли реальное поведение.

### E1.2 Одна кнопка, одно название, везде

- [x] a. Выбрать название. Предлагаю **«Как это сделать?»**. Согласовать до начала — переименование трогает 12 браузерных наборов.
- [x] b. Свести три сущности к одной: «Покажи, как» ([`Exercise.tsx:64`](../src/components/Exercise.tsx:64), [`GestureCoach.tsx:123`](../src/components/GestureCoach.tsx:123)), «Покажи подсказку» (второй `CoachButton` на части заданий), «+ Нужна подсказка?» ([`Exercise.tsx:286`](../src/components/Exercise.tsx:286)). Текстовая подсказка `block.hint` становится последним шагом той же подсказки, а не отдельной ссылкой.
- [x] c. Кнопка показывается на **каждом** задании независимо от истории (сейчас — зависит от семейства жеста).
- [x] d. Обновить строки в тестах: `activity-smoke`, `child-revisions-smoke`, `coach-layout-smoke`, `coach-stability-smoke`, `composition-pairs-smoke`, `full-course-smoke`, `manual-feedback-smoke`, `mobile-coach-smoke`, `onboarding-gesture-regressions`, `source-feedback-smoke`, `source-fidelity-smoke`, `teaching-smoke`, `tests/task-teaching.test.ts`. Одним коммитом с переименованием.
- [x] e. **Тест (браузер):** `all-pages-smoke` дополнить проверкой «на каждом из 1323 блоков ровно одна кнопка подсказки».

### E1.3 Не показывать на пройденном

После E1.1 автопоказа нет, и жалоба снимается сама. Остаётся закрепить тестом.

- [x] a. **Тест (браузер):** решить задание → уйти → вернуться → подсказка не открылась сама. В `gesture-coach-smoke`.

### E1.4 Вернуть прокрутку после закрытия

- [x] a. В [`App.tsx:693`](../App.tsx:693) `revealCoachTarget` — запомнить `scrollY` перед прокруткой к цели; после закрытия подсказки вернуть.
- [x] b. Проверить web и native: на web прокручивается `lesson-scroll-pane`, на native — та же `ScrollView`; `Platform.OS !== "web"` ветка в `GestureCoach.tsx:~420` уже различает их.
- [x] c. **Тест (браузер):** `scrollY` до открытия и после закрытия отличаются не больше чем на 8 px. В `coach-stability-smoke`.

### E1.5 По центру и крупнее

- [x] a. Карточку из верхнего края перенести в центр экрана; на телефоне — низ экрана (большой палец), на планшете/десктопе — центр. Это `CoachSurface.tsx` / оверлей в `GestureCoach.tsx`.
- [x] b. Визуальная часть (анимация жеста) — не меньше 40 % высоты карточки; текст — одна строка, крупно.
- [x] c. Обновить `coach-layout-smoke` (он проверяет позиции) и эталоны `docs/coach-*-{390,800,1280}.png`.
- [x] d. **Тест (браузер):** карточка не перекрывает шапку и заголовок страницы; на 390 не выходит за экран.

**Сделано 25.09.2026** одним коммитом (правки взаимозависимы: одна кнопка требует шагов-жестов внутри общего объяснения, а те — стабильной карточки). По E1.5: карточка больше не ставится поверх шапки ни на телефоне, ни на десктопе (баг геометрии: верхний кандидат считал `y = gap` без учёта шапки); по центру она стояла и раньше, когда не перекрывала цель; текст 17 → 18 px, карточка на десктопе 320 → 360 px. Заодно найден и закрыт переходный кадр: до первого `onLayout` карточка позиционировалась по угаданной высоте 240 и на мгновение вылезала за экран — теперь до замера она невидима и прижата к низу. Второй попутный дефект: восстановление прокрутки после закрытия на web ломал сам `Modal` react-native-web — он возвращает фокус кнопке «Как это сделать?» на кадр позже закрытия, и браузер докручивал кнопку в кадр поверх восстановленной позиции; сигнал «закрыто» теперь идёт из `onDismiss`. «Крупнее визуальную часть» на телефоне упирается в место под демонстрацию; вернуться после повторного теста.

**Ворота E1 дополнительно:** `gesture-coach-smoke`, `coach-layout-smoke`, `coach-stability-smoke`, `mobile-coach-smoke`, `onboarding-*` — все переписаны под новое поведение, ни один не удалён.

---

## E2. Мелкие цели на телефоне

Закрывает пункт 3 сводки. На 375×812 клетка ≈ 29 px, допуски 6–14 px.

### E2.1 Допуски с полом в пикселях

- [x] a. `matchesTrace` в [`src/lib/tracing.ts:56`](../src/lib/tracing.ts:56) принимает необязательный `cellPx` (физический размер клетки). Все допуски считаются как `max(допуск_в_клетках, минимум_px / cellPx)`. Без `cellPx` — поведение прежнее, чтобы Node-тесты не поменялись.
- [x] b. Минимумы: точка — радиус 22 px; начало/конец линии — 16 px; средняя ошибка — 10 px. Числа — стартовые, уточнить на устройстве.
- [x] c. `DrawingPad.tsx:77` уже считает `cellSize` — передать его в проверку.
- [x] d. **Тест (Node):** те же штрихи, что в `copybook-source.test.ts`, при `cellPx = 29` проходят с отклонением 12 px, при `cellPx = 60` — нет. Плюс: при отсутствии `cellPx` все существующие тесты (`interactions`, `copybook-source`, `child-revisions`, `manual-feedback`, `full-course`) не тронуты.

### E2.2 Точка — тап, а не штрих

- [x] a. Для `target.dot` принимать любое касание внутри клетки цели, независимо от длины штриха (сейчас длина < 0,65 клетки = 19 px на телефоне, палец столько «едет» сам).
- [x] b. **Тест (Node + браузер):** тап с дрожанием 25 px засчитывается; тап в соседней клетке — нет.

### E2.3 Крупная клетка на узком экране

Сетки 12×8 привязаны к сканам (чёрточки на сторонах клеток, наклонные палочки 1×2), поэтому **число клеток менять нельзя** — сломается геометрия и `copybook-source`.

- [x] a. На ширине < 600: поле рисования шире экрана с горизонтальной прокруткой, клетка не меньше 44 px (12 × 44 = 528 px). Прокрутка блокируется на время штриха (`onDrawing` уже отключает вертикальную — расширить на горизонтальную).
- [ ] b. Альтернатива для обсуждения: пинч-зум поля. Не понадобилась: самая широкая цель — 2,4 клетки, лист сам подъезжает к цели.
- [ ] c. Проверить на настоящем телефоне (в Chromium 390×844 с touch — сделано: клетка 44 px, штрихи принимаются, лист не двигается). Раскатано на все графические блоки сразу: правило одно, `MIN_CELL_PX`.
- [x] d. **Тест (браузер, 390):** размер клетки ≥ 44 px, штрих через touch засчитывается, вертикальная прокрутка страницы во время штриха не двигается. В `drawing-guidance-smoke`.

**Сделано 25.09.2026** одним коммитом. Пороги в пикселях: точка 24/32, концы 18, средняя ошибка 12, выброс 22, замыкание 22 (стартовые, уточнить на устройстве). Штрих хранит `cellPx`, поэтому проверка сохранённого ответа даёт тот же вердикт, что и поле в момент рисования.

**Ворота E2 дополнительно:** `tests/trace-directions.test.ts`, `copybook-source.test.ts` без изменений ожиданий; `python3 scripts/render-handwriting-comparison.py` — наложение на оригиналы не поплыло.

---

## E3. Мелкие баги

Пункты П2 сводки. Дёшево, заметно, одним заходом.

### E3.1 `□` → пустое поле

`□` приходит из трёх мест: runtime-подстановка ([`CourseTask.tsx:169,195`](../src/components/CourseTask.tsx:169), [`StoryTask.tsx:70,178,220`](../src/components/StoryTask.tsx:70)), 14 блоков контента (`source_blocks.json` → `fullBookData.ts`) и примеры обучения в `taskTeaching.ts` («3 + □ = 5», зафиксировано в `task-teaching.test.ts:246-351`).

- [x] a. Не менять данные: `□` остаётся токеном. Менять отрисовку: везде, где текст с `□` выводится, токен рендерится как рамка-пропуск (пустой квадрат с рукописной обводкой в стиле тетради), а не как символ шрифта.
- [x] b. Один компонент `Blank` и одна функция разбиения строки на текст/пропуски; использовать в `CourseTask`, `StoryTask`, `CoachExample`, `Exercise`.
- [x] c. **Тест (Node):** разбиение «4 + □ = 6» → `["4 + ", blank, " = 6"]`. **Тест (браузер):** на странице с неизвестным слагаемым в DOM нет символа U+25A1.

### E3.2 Дубль заголовка

- [x] a. [`Exercise.tsx:71`](../src/components/Exercise.tsx:71): для `kind="read"` не печатать `title` второй раз (он уже в `App.tsx:683`).
- [x] b. [`Exercise.tsx:68`](../src/components/Exercise.tsx:68): не печатать `prompt`, если он равен `title` после нормализации пробелов и точки в конце (120 блоков).
- [x] c. Проверить, что `onboarding-*` и `all-pages-smoke` не ищут текст задания по второму вхождению.
- [x] d. **Тест (Node):** по `fullBookData` — ни у одного блока отрисованный заголовок не равен отрисованному заданию. **Тест (браузер):** страница 3, шаг 1 — «Первый десяток» встречается один раз.

### E3.3 Поиск

- [x] a. № упражнения показан в шапке шага («Шаг 2 из 6 · № 500»). В точки шагов не добавлен: на их `accessibilityLabel` завязаны тесты, а номер уже виден рядом.
- [x] b. Результат поиска по «№ N» открывает нужную страницу **и нужный шаг**, а не первый.
- [x] c. В выдаче подписывать, что нашли: «стр. 47 · № 500». Поиск без «№» — по номеру страницы и заголовку, как сейчас, но с подписью «страница».
- [x] d. Плейсхолдер и подсказка под полем: «Номер страницы (80) или задания (№ 500)».
- [x] e. **Тест (браузер):** ввод «№ 500» → открыт блок с `exerciseNumber === 500`, номер виден в заголовке шага. Ввод «50» → открыта страница 50. В `browser-smoke` или `full-course-smoke` (там уже есть навигация по `exerciseNumber`).

### E3.4 Задания, которые приложение не проверяет

«Сделай 2 шага вперёд» — это `activity.mode = "sequence"` (27 блоков), а не физическое задание; Алексей увидел «подсчёт шагов не готово». Задания «измерьте класс шагами» — `kind = "practical"`.

- [x] a. Аудит: пройти все 27 `sequence`-блоков и `practical`-блоки без проверяемого результата, записать список в `docs/` — что из них выглядит недоделкой и почему.
- [x] b. Не понадобилось: аудит показал, что все 53 практических блока проверяемы (режимы place/draw/construct/cards), а «недоделкой» выглядели 27 `sequence`-блоков — см. [HANDS_ON_TASKS.md](../docs/HANDS_ON_TASKS.md).
- [x] c. Для `sequence` — решить по результатам аудита: либо доработать отображение, либо исключить те, что без картинки и смысла на экране.
- [x] d. **Тест (Node):** каждый блок без проверяемого ответа имеет `kind`/флаг «сделай в жизни»; ни один не показывает «Проверить».

### E3.5 Скриншоты Алексея

- [ ] a. Запросить у Алексея «УЧП…», «вёрстка поехала», «область чуть мимо», «на 2». Завести задачи по факту. **Блокер внешний.**

**Сделано 25.09.2026** одним коммитом. Заголовок блока переехал из `App` в `Exercise` (одна отрисовка, коуч видит цель, в E4 туда же встанет ✓).

---

**→ После E3: PR в `main`, выкладка, повторный тест теми же людьми.** Это снимет основной шум и покажет, что осталось. E4–E6 можно делать параллельно с ожиданием отзывов.

---

## E4. Статус выполнения

Пункт 6 сводки.

- [x] **E4.1** Решение по цвету: зелёного в «тетради» нет намеренно. Предложение — успех показывать формой и красной галочкой «учительской ручки». **Согласовать до кода.**
- [x] **E4.0** Попутно из E1: у `Button small` дробная высота (паддинги 8 + текст 17 px без явного `lineHeight` → кнопка на `y = 18.5`), из-за чего округление прокрутки «плавает» на 1 px и пиксельно-точные проверки вёрстки становятся хрупкими. Задать `lineHeight: 24` подписи кнопки — высота 40/50 px ровно.
- [x] **E4.2** В [`Controls.tsx`](../src/components/Controls.tsx) добавить `Button` вариант `done`: плоский, без нижней «губы», с галочкой, `accessibilityState={{ disabled: true }}`; строка «✓ Получилось!» не меняется (на неё завязаны тесты).
- [x] **E4.3** Применить в `Exercise.tsx:253`, `CourseTask.tsx:753`, `NumberGameTask.tsx:69,75`, `StoryTask.tsx:239`, `PracticalTask.tsx:372`.
- [x] **E4.4** ✓ у заголовка задания в `App.tsx:683`, когда `isDone`.
- [x] **E4.5** **Тест (браузер):** после верного ответа кнопка имеет `disabled` и не реагирует на клик; у заголовка появляется ✓. **Скриншот** для сравнения с E0.3.

**Сделано 25.09.2026.** Цвет: без зелёного — форма (плоская кнопка) и красная галочка. `NumberGameTask` оставлен как есть (см. AUTOMATED_TESTING.md).

---

## E5. Шапка и вёрстка

Пункты 5 и 7 сводки.

- [x] **E5.1** Проще, чем планировалось: на ширине < 1000 шапка не закреплена вовсе — она первый элемент листа и уезжает с ним (буквально «не стики», как просила Таня). На десктопе — без изменений.
- [ ] **E5.2** «Навбар улетает, страница обрезается» — не воспроизведено на 768×1024. Сделано превентивно: корневая высота `100dvh` на web, где поддерживается. Остаётся проверить на устройстве Тани.
- [x] **E5.3** Выбран вариант с компактной иллюстрацией: на ширине < 600 картинка не выше 180 px (240 для «рассмотри»), картинка и ответ помещаются вместе. Закреплённый вопрос не делался — ребёнок не читает, вопрос звучит голосом (E7).
- [x] **E5.4** **Тест (браузер, 390 и 768):** прокрутка на 300 px вниз → шапка вне экрана; вверх → вернулась; при закреплённом вопросе поле ответа видно вместе с ним. Обновить `onboarding-all-views`.

**Сделано 25.09.2026** одним коммитом.

---

## E6. Тексты и понятность

Пункт 4 сводки. Редакторская работа; делается **одним проходом с подготовкой текстов для озвучки (E7.1)** — это один и тот же обход контента.

- [x] **E6.1** Правило и линтер: `title` — о чём страница/шаг (существительное), `prompt` — что делать (глагол в повелительном наклонении, ≤ 12 слов). Скрипт в `scripts/content/` печатает нарушения; сначала как отчёт, потом как `--check`.
- [x] **E6.2** Для нумерованных блоков заголовок выводится из роли в ТЗ («Задача», «Примеры на сложение», «Упражнение»…), голые столбики получили «Реши примеры и запиши ответы.». Дубли `title == prompt` на экране скрыты ещё в E3.
- [ ] **E6.3** Подсказки: отчёт для редактора готов ([HINTS_AUDIT.md](../docs/HINTS_AUDIT.md), 196 блоков страниц 3–29). Сами правки текстов — редакторская работа, которую нельзя делать без сверки со сканом; в коде не выполнены.
- [x] **E6.4** Перегенерация (`extract.py`, `build_course.py`), `npm run test:content` зелёный, `page-specs`/`content` тесты — номера и иллюстрации не потеряны.

**Сделано 25.09.2026** в части кода: линтер, отчёты, механические правила генератора (488 → 245 замечаний). Оставшиеся 245 — редактура текста по сканам, см. [TEXT_LINT.md](../docs/TEXT_LINT.md).

---

## E7. Озвучка

> **Приостановлено 25.09.2026 по решению пользователя.** Работа с голосом остановлена, кнопка озвучки («♫ Слушать» в задании и «Послушать» в подсказке) временно убрана из интерфейса, прямые вызовы `expo-speech` удалены (зависимость оставлена). Готовый черновик E7 — произносимые формы (`spoken.ts`), ядро диктора с очередью и манифестом записей (`narration.ts`), автоозвучивание с выключателем для родителей, конвейер записей (`scripts/tts/`) и браузерный сценарий — сохранён в ветке `wip/narration` (от коммита E6) с зелёными юнит-тестами; к нему можно вернуться, когда будет выбран голос.

Пункт 1 сводки. Главный блокер для целевого возраста и самый длинный эпик. База — [VOICE_SELECTION.md](../docs/VOICE_SELECTION.md).

### E7.1 Модель контента

- [ ] a. В `types.ts` — `spoken?: { title, prompt, fields[], hint, success, retry }` у блока. Экранный текст и текст диктора раздельно.
- [ ] b. Генератор произносимой формы в `scripts/content/spoken.py`: «2 + 1 = 3» → «два плюс один равно три», «1 см» → «один сантиметр», согласование с существительным (один кружок / два кружка / пять кружков). Список исключений и ударений — отдельный словарь `spoken_overrides.py`.
- [ ] c. **Тест (Node/Python):** таблица числительных и падежей 1–100, арифметика всех операций, единицы измерения. Самая тестируемая часть эпика — писать тесты первыми.

### E7.2 Голос и генерация

- [ ] a. Сравнительное прослушивание по списку фраз из VOICE_SELECTION.md: SpeechKit `marina`/`dasha` vs ElevenLabs. Выбрать один. **Решение человека, не кода.**
- [ ] b. `scripts/tts/generate.py`: вход — `spoken`-тексты, выход — `assets/audio/<hash>.mp3` + манифест `{hash: {text, voice, settings}}`. Кеш по хешу: при правке текста перегенерируется только затронутое. Ключ сервиса — только в окружении скрипта, не в сборке.
- [ ] c. Оценить объём: ~1240 блоков × 3–6 фраз × ~40 КБ ≈ 150–300 МБ. Это не влезает в web-сборку целиком — грузить по страницам (lazy), проверить лимиты Render ([DEPLOY_RENDER.md](../docs/DEPLOY_RENDER.md)) и размер `dist`.
- [ ] d. **Тест:** манифест покрывает все `spoken`-тексты (ни одной фразы без файла), `prepare_assets.py` не ломается.

### E7.3 Плеер

- [ ] a. Один плеер вместо двух (`App.tsx:242` и `GestureCoach.tsx:1043`): очередь фраз, стоп при смене шага, без наложения голосов.
- [ ] b. Порядок: заголовок → задание → подписи полей; после проверки — результат. Подсказка — через тот же плеер.
- [ ] c. Fallback на `expo-speech`, если файла нет (офлайн, новая фраза без генерации) — с прежними настройками `ru-RU`.
- [ ] d. **Тест (браузер):** при открытии шага запрошен ровно один аудиофайл из манифеста; при переходе на следующий шаг предыдущий остановлен (`network` + `console`). **Тест (Node):** очередь — порядок и стоп.

### E7.4 Автозапуск и управление

- [ ] a. Автозапуск при открытии задания; кнопка «♫ Слушать» становится «повторить». Переключатель «без звука» в «Родителям», запоминается.
- [ ] b. На web автозапуск звука до первого касания запрещён браузером — первый запуск после «Начать заниматься», дальше свободно. Проверить на iOS Safari отдельно.
- [ ] c. **Тест (браузер):** после нажатия «Начать заниматься» звук стартует без клика по «Слушать»; при выключенном звуке — нет запросов аудио.

**Объём:** L. **Коммиты:** «Add spoken text model and number-to-words generator» (E7.1), «Generate and cache pre-recorded speech» (E7.2), «Play pre-recorded speech through one player» (E7.3), «Speak tasks automatically» (E7.4).

**Ворота E7 дополнительно:** прослушать 20 случайных страниц человеком; проверка на настоящем iOS и Android (стоп при смене задания, режим без звука, первый запуск).

**→ После E7: PR в `main`, выкладка, третий тест — уже с детьми.**

---

## E8. Предложения (после повторного теста)

Не декомпозирую до решения, что берём.

- [ ] **E8.1** Ввод цифры с клавиатуры как альтернатива рисованию (Влад). S. Заодно спасает тех, у кого E2 не помогло.
- [ ] **E8.2** Написать словом («ТРИ»). S.
- [ ] **E8.3** Произнести и послушать себя — запись/воспроизведение без оценки. S. Распознавание речи — не брать.
- [ ] **E8.4** Ачивки за раздел (Таня) — штамп «Проверено» в стиле тетради при закрытии раздела. M.
- [ ] **E8.5** Рамка задания (Гордин) — вместо «как в мессенджере» оформление «поля тетради». Обсудить. S.
- [ ] **E8.6** Чистка сканов: апскейл + убрать растр без перерисовки. Пилот на 10 картинках, показать тестерам. M, в основном ручная.

---

## Сводная таблица

| Эпик | Что | Объём | Коммитов | Внешний блокер |
| --- | --- | --- | --- | --- |
| E0 | Базовая линия | S | 1 | — |
| E1 | Подсказки | M | 4 | название кнопки (E1.2a) |
| E2 | Мелкие цели | M | 2 | проверка на телефоне (E2.3c) |
| E3 | Мелкие баги | S–M | 4 | скриншоты Алексея (E3.5) |
| — | **PR, выкладка, повторный тест** | | | |
| E4 | Статус выполнения | S | 1 | решение по цвету (E4.1) |
| E5 | Шапка и вёрстка | M | 2 | устройство Тани (E5.2) |
| E6 | Тексты | M–L | по разделам | — |
| E7 | Озвучка | L | 4 | выбор голоса (E7.2a) |
| — | **PR, выкладка, тест с детьми** | | | |
| E8 | Предложения | — | — | решение, что берём |

Внешние блокеры не останавливают эпик: E1.2a и E4.1 решаются до начала эпика одним сообщением; E2.3c, E3.5, E5.2 — параллельно с работой.
